# Working notes for Claude

- Keep replies to the owner minimal: do the work, report only the PR link and anything that needs their action. No explanations unless asked.
- Edit `src/*` and `scripts/build.py`, then run `python3 scripts/build.py && python3 scripts/validate.py`. Never hand-edit `index.html` or `the-yellow-bottle.xml`.
- No hover motion on arrows or buttons (colour changes are fine).
